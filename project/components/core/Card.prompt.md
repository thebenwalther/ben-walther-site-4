Generic surface container with token-mapped padding, radius, shadow, and three background treatments.

```jsx
<Card surface="white" pad="lg" radius="lg">…</Card>
<Card surface="dark">…</Card>
```

`surface`: white / cream / dark. `pad`: sm/md/lg. `radius`: sm/md/lg/xl. `shadow`: none/sm/md/lg (forced off on dark).
